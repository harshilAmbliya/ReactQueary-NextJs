import AddUsers from "@/components/AddUsers";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <AddUsers />
    </>
  );
};

export default page;
